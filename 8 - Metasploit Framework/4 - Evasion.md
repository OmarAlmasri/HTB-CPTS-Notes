```ad-note
While using **msfvenom**, we can use the `-k` option to backdoor a specific file.

---

Archive the payload twice and add passwords to both archives. Remove the `.rar / . zip / .tar.gz / .7z` extension from their names. This will help bypassing lots of security vendors.
```

